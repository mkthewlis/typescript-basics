// There is the option to be specific, but better to allow TS to infer value types
// const person: {
//     name: string;
//     age: number;
// } = {
const person = {
    name: 'Bob',
    age: 28
};

console.log(person);